/**
 * Configuration NextAuth.js v5
 * Authentification locale uniquement (je suis le seul utilisateur)
 * Sera complétée à l'étape Auth avec le formulaire de login
 */

// TODO: Compléter la configuration NextAuth.js v5
// L'implémentation complète sera faite à l'étape 4 (Layout & Navigation)
// avec le formulaire de login et la session

export const authConfig = {
  // Configuration NextAuth v5 — à compléter
  pages: {
    signIn: "/login",
  },
};
